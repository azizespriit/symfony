<!DOCTYPE html>
<html lang="en">

<head>
    <title>Sportify</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <link rel="stylesheet" href="{{ asset('frontoffice/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('frontoffice/css/style.css') }}">
    
    <style>
        .chat-button {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: all 0.3s;
            z-index: 1000;
            text-decoration: none;
        }
        
        .chat-button svg {
            width: 30px;
            height: 30px;
            fill: white;
        }
        
        .chat-button:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        }
        
        body {
            background-color: #f8f9fa;
        }
        
        .simple-header {
            background: #343a40;
            color: white;
            padding: 15px 0;
        }
        
        .simple-header .container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .simple-header .logo {
            font-size: 24px;
            font-weight: bold;
        }
        
        .simple-header .logo span {
            color: #6a11cb;
        }
        
        .simple-header .nav-links {
            display: flex;
            gap: 20px;
        }
        
        .simple-header .nav-links a {
            color: white;
            text-decoration: none;
        }
        
        .simple-header .nav-links a:hover {
            color: #6a11cb;
        }
        
        .main-content {
            padding: 40px 0;
            min-height: calc(100vh - 130px);
        }
        
        .simple-footer {
            background: #343a40;
            color: white;
            padding: 15px 0;
            text-align: center;
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>

<body>
    <header class="simple-header">
        <div class="container">
            <div class="logo">SPORT<span>IFY.</span></div>
            <div class="nav-links">
                <a href="#">Home</a>
                <a href="#">Services</a>
                <a href="{{ path('app_user_profile', {'id_user': app.user.id_user}) }}">Profile</a>
                <a href="{{ path('app_logout') }}">Log out</a>
            </div>
        </div>
    </header>

    <div class="main-content">
        <div class="container">
            {% block body %}{% endblock %}
        </div>
    </div>

    <footer class="simple-footer">
        <div class="container">
            <p>&copy; {{ "now"|date("Y") }} Sportify. All Rights Reserved.</p>
        </div>
    </footer>

    <!-- Chatbot button with fixed URL -->
    <a href="{{ path('app_front_chatbot') }}" class="chat-button" title="Chat with our AI assistant">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
            <path d="M12 1c-6.628 0-12 4.573-12 10.213 0 2.39.932 4.591 2.427 6.164l-2.427 5.623 7.563-2.26c9.495 2.598 16.437-3.251 16.437-9.527 0-5.64-5.372-10.213-12-10.213z"/>
        </svg>
    </a>

    <!-- Core Scripts -->
    <script src="{{ asset('frontoffice/js/jquery-3.3.1.min.js') }}"></script>
    <script src="{{ asset('frontoffice/js/bootstrap.min.js') }}"></script>
    
    {% block javascripts %}{% endblock %}
</body>
</html>