<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Publication PDF</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; color: #333; }
        header { border-bottom: 2px solid #555; margin-bottom: 20px; padding-bottom: 10px; }
        header h1 { font-size: 24px; margin: 0; }
        .publication { border: 1px solid #ccc; border-radius: 8px; padding: 15px; }
        .publication img { 
            max-width: 400px; 
            height: auto; 
            margin: 15px auto; 
            display: block; /* centre l'image */
        }
        h2 { font-size: 20px; margin-top: 10px; color: #444; }
        p { line-height: 1.5; margin: 8px 0; }
        .meta { font-size: 12px; color: #666; margin-top: 15px; }
    </style>
</head>
<body>
    <header>
        <h1>Publication</h1>
    </header>

    <div class="publication">
        {% if imagePath %}
            <img src="{{ imagePath }}" alt="Image de publication">
        {% else %}
            <p><em>Aucune image disponible.</em></p>
        {% endif %}

        <h2>{{ publication.description }}</h2>
        <p>{{ publication.contenu }}</p>
        <p class="meta"><strong>Publié le :</strong> {{ publication.datePub|date('d M Y') }}</p>
    </div>
</body>
</html>
