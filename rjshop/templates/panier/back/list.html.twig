{% extends 'baseback.html.twig' %}

{% block title %}Admin - Carts List{% endblock %}

{% block body %}
<div class="row tm-content-row tm-mt-big">
    <div class="col-12 tm-block-col">
        <div class="bg-white tm-block h-100">
            <div class="row">
                <div class="col-md-8 col-sm-12">
                    <h2 class="tm-block-title d-inline-block">Shopping Carts</h2>
                </div>
                <div class="col-md-4 col-sm-12 text-right">
                    <a href="{{ path('panier_new') }}" class="btn btn-small btn-primary">Create New Cart</a>
                </div>
            </div>
            <div class="table-responsive">
                <table class="table table-hover table-striped tm-table-striped-even mt-3">
                    <thead>
                        <tr class="tm-bg-gray">
                            <th scope="col">ID</th>
                            <th scope="col">Total Price</th>
                            <th scope="col">Status</th>
                            <th scope="col" class="text-center">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for panier in paniers %}
                        <tr>
                            <td>{{ panier.id }}</td>
                            <td>{{ panier.prixTotal|number_format(2, '.', ',') }} €</td>
                            <td>
                                {% if panier.commande %}
                                    <span class="badge badge-success">Ordered</span>
                                {% else %}
                                    <span class="badge badge-warning">Pending</span>
                                {% endif %}
                            </td>
                            <td class="text-center">
                                <a href="{{ path('panier_show', {'id': panier.id}) }}" class="tm-product-delete-link">
                                    <i class="far fa-eye tm-product-delete-icon"></i>
                                </a>
                            </td>
                        </tr>
                        {% else %}
                        <tr>
                            <td colspan="4" class="text-center">No carts found</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block css %}
<style>
    .tm-product-delete-link {
        padding: 10px;
        color: #6c757d;
    }
    .tm-product-delete-icon {
        font-size: 1.1rem;
    }
    .badge {
        padding: 5px 10px;
    }
    .badge-success {
        background-color: #28a745;
    }
    .badge-warning {
        background-color: #ffc107;
        color: #212529;
    }
    .badge-info {
        background-color: #17a2b8;
    }
    .badge-secondary {
        background-color: #6c757d;
    }
</style>
{% endblock %}
